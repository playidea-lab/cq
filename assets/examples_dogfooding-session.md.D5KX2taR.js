import{_ as s,o as a,c as t,a2 as n}from"./chunks/framework.DdLJaIXX.js";const u=JSON.parse('{"title":"Dogfooding Session: Wire Smoke Validation","description":"","frontmatter":{},"headers":[],"relativePath":"examples/dogfooding-session.md","filePath":"examples/dogfooding-session.md"}'),i={name:"examples/dogfooding-session.md"};function o(p,e,l,r,d,c){return a(),t("div",null,[...e[0]||(e[0]=[n(`<h1 id="dogfooding-session-wire-smoke-validation" tabindex="-1">Dogfooding Session: Wire Smoke Validation <a class="header-anchor" href="#dogfooding-session-wire-smoke-validation" aria-label="Permalink to &quot;Dogfooding Session: Wire Smoke Validation&quot;">​</a></h1><p>This is a real example of how CQ was used to build CQ&#39;s own wire validation system. Every step — from exploration to commit — was orchestrated through CQ itself, on the same codebase being changed.</p><hr><h2 id="step-1-pi-—-exploring-the-problem" tabindex="-1">Step 1: /pi — Exploring the Problem <a class="header-anchor" href="#step-1-pi-—-exploring-the-problem" aria-label="Permalink to &quot;Step 1: /pi — Exploring the Problem&quot;">​</a></h2><p>The question was simple but the answer wasn&#39;t obvious:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/pi &quot;wire가 실제로 prod에서 작동하는지 확인하는 방법&quot;</span></span></code></pre></div><p><code>/pi</code> spins up a research loop. It pulls relevant knowledge from the knowledge base, scans recent traces, and drafts an idea document. The AI explored three approaches:</p><p><strong>L2 Smoke</strong> — spawn the MCP stdio process, dispatch a known-safe tool call, verify the response schema. Fast (under 5 seconds), no side effects, runs in CI.</p><p><strong>L3 Canary</strong> — deploy a shadow instance alongside prod, route 1% of real traffic through it, diff responses. Accurate but requires infra overhead.</p><p><strong>Breadth-first dispatch</strong> — call every registered tool with a minimal payload, check for panics and schema drift. Comprehensive but slow.</p><p>The discussion converged on L2 smoke as the right first layer:</p><blockquote><p>&quot;A canary tells you what broke in production. A smoke test tells you whether production is even reachable. You need the smoke test first.&quot;</p></blockquote><p>Output: <code>.c4/ideas/wire-smoke-validation.md</code> — a structured idea document with problem statement, constraints, and a proposed implementation sketch.</p><hr><h2 id="step-2-c4-plan-—-design-tasks" tabindex="-1">Step 2: /c4-plan — Design + Tasks <a class="header-anchor" href="#step-2-c4-plan-—-design-tasks" aria-label="Permalink to &quot;Step 2: /c4-plan — Design + Tasks&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/c4-plan</span></span></code></pre></div><p><code>/c4-plan</code> reads the idea document and converts it into EARS-format requirements, then generates a task graph.</p><p><strong>Requirements generated:</strong></p><ul><li>WHEN the binary is rebuilt, the wire smoke test SHALL verify all registered MCP tools respond within 2 seconds</li><li>WHERE a tool returns a non-200 response or schema mismatch, the script SHALL exit non-zero and print the failing tool name</li><li>IF <code>--all</code> flag is passed, the script SHALL test every tool; otherwise only the <code>c4_status</code> sentinel</li></ul><p><strong>Design decisions recorded:</strong></p><table tabindex="0"><thead><tr><th>Decision</th><th>Choice</th><th>Rationale</th></tr></thead><tbody><tr><td>Script vs inline Go</td><td>Shell script</td><td>Portable, no recompile needed, readable in CI logs</td></tr><tr><td>Hard fail vs soft</td><td>Hard fail</td><td>Silent failures are worse than noisy ones</td></tr><tr><td>MCP transport</td><td>stdio</td><td>Same path as real agent connections</td></tr></tbody></table><p><strong>Three tasks created:</strong></p><ul><li><code>T-WSV-01</code>: Write <code>wire-smoke.sh</code> — spawn stdio MCP, dispatch tool calls, verify responses</li><li><code>T-WSV-02</code>: Integrate into <code>/c4-finish</code> as Step 6.5 — run smoke test before commit</li><li><code>T-WSV-03</code>: Integration test — full <code>--all</code> run against local build, 0 failures required</li></ul><p>Dependencies: T-WSV-02 blocked on T-WSV-01. T-WSV-03 blocked on T-WSV-02.</p><hr><h2 id="step-3-c4-run-—-implementation" tabindex="-1">Step 3: /c4-run — Implementation <a class="header-anchor" href="#step-3-c4-run-—-implementation" aria-label="Permalink to &quot;Step 3: /c4-run — Implementation&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/c4-run</span></span></code></pre></div><p>Workers spawned. Each picked up one task from the queue.</p><p><strong>T-WSV-01</strong> — the worker read the idea document, checked existing MCP handler registration patterns in <code>c4-core/internal/mcp/</code>, and wrote <code>scripts/wire-smoke.sh</code>. The script:</p><ol><li>Builds the binary (<code>make install</code>)</li><li>Spawns <code>cq serve</code> in stdio mode with <code>--smoke</code> flag</li><li>Sends a JSON-RPC <code>tools/call</code> for each registered tool</li><li>Validates the response envelope matches the MCP schema</li><li>Reports pass/fail per tool, exits 1 on any failure</li></ol><p><strong>T-WSV-02</strong> — unblocked as soon as T-WSV-01 completed. The worker found the <code>/c4-finish</code> skill definition and inserted Step 6.5 between &quot;build verification&quot; and &quot;commit&quot;:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Step 6.5: Wire smoke validation</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [ </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-f</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> scripts/wire-smoke.sh ]; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">then</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  bash</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> scripts/wire-smoke.sh</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --sentinel</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ||</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> exit</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">fi</span></span></code></pre></div><p><strong>T-WSV-03</strong> — the integration test worker ran the full suite:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Testing 183 tools... [183/183] PASS</span></span>
<span class="line"><span>Wire smoke: 0 failures</span></span></code></pre></div><p>All three tasks completed. No human intervention between task spawn and final result.</p><hr><h2 id="step-4-c4-finish-—-verify-commit" tabindex="-1">Step 4: /c4-finish — Verify + Commit <a class="header-anchor" href="#step-4-c4-finish-—-verify-commit" aria-label="Permalink to &quot;Step 4: /c4-finish — Verify + Commit&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/c4-finish</span></span></code></pre></div><p>The finish skill runs its standard checklist:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Step 1: git status — 4 files modified, 1 new file</span></span>
<span class="line"><span>Step 2: go build ./... — OK</span></span>
<span class="line"><span>Step 3: go vet ./... — OK</span></span>
<span class="line"><span>Step 4: go test ./... — OK (2m14s)</span></span>
<span class="line"><span>Step 5: Lint — OK</span></span>
<span class="line"><span>Step 6.5: Wire smoke (sentinel) — PASS [cq_status responded in 341ms]</span></span>
<span class="line"><span>Step 7: Commit</span></span></code></pre></div><p>Commit message:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>feat(infra): wire-smoke.sh L2 validation</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Add shell-based smoke test that spawns MCP stdio and verifies</span></span>
<span class="line"><span>tool dispatch end-to-end. Integrated into /c4-finish as Step 6.5.</span></span>
<span class="line"><span>183 tools validated, sentinel mode for fast CI path.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Closes T-WSV-01 T-WSV-02 T-WSV-03</span></span></code></pre></div><hr><h2 id="what-happened-next" tabindex="-1">What Happened Next <a class="header-anchor" href="#what-happened-next" aria-label="Permalink to &quot;What Happened Next&quot;">​</a></h2><p>The same <code>/pi → /c4-plan → /c4-run → /c4-finish</code> pattern was applied to adjacent systems:</p><p><strong>CF Worker smoke test</strong> (<code>cf-smoke.sh</code>) — validates that the Cloudflare Worker serving the Streamable MCP surface returns correct tool schemas. Added to deployment pipeline.</p><p><strong>Edge Functions smoke test</strong> (<code>edge-smoke.sh</code>) — validates Supabase Edge Functions. On its first run against production, it caught a live bug: <code>telegram-notify</code> was returning 200 even on authentication failure (fail-open instead of fail-closed). The bug had been silent for 11 days.</p><p><strong>L3 canary in GitLab CI</strong> — after L2 smoke proved stable, the canary layer was added. It routes synthetic tool calls through the production relay and diffs responses against a known-good snapshot. Runs nightly.</p><hr><h2 id="key-takeaway" tabindex="-1">Key Takeaway <a class="header-anchor" href="#key-takeaway" aria-label="Permalink to &quot;Key Takeaway&quot;">​</a></h2><p>&quot;CQ builds CQ&quot; is not a slogan. The wire validation system that now protects every CQ deployment was itself built through the same loop it enforces:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/pi — understand the problem</span></span>
<span class="line"><span>/c4-plan — make the design explicit</span></span>
<span class="line"><span>/c4-run — implement without drift</span></span>
<span class="line"><span>/c4-finish — verify before shipping</span></span></code></pre></div><p>The edge-smoke finding — a silent auth failure in production — is the clearest evidence that the system works. The bug wasn&#39;t caught in code review. It wasn&#39;t caught in unit tests. It was caught by running the actual wire under controlled conditions and checking what came back.</p><p>That is what wire validation is for. And that is what CQ is for.</p>`,54)])])}const g=s(i,[["render",o]]);export{u as __pageData,g as default};
