description = "Clear C4 state completely (Reset project)."

prompt = """
1. Ask the user for confirmation (this action is destructive).
2. Call `c4_clear(confirm=true)` to delete the .c4 directory and clear the daemon cache.
"""
