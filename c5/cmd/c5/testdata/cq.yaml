run: "echo pipeline-ok"
uv: false  # pipeline tests run without uv; real ML jobs omit this (default true)

artifacts:
  input:
    - name: input-dataset
      local_path: data/input.bin
  output:
    - name: output-model
      local_path: results/model.bin
