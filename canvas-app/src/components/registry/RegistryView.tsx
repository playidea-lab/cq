interface RegistryViewProps {
  projectPath: string;
}

export function RegistryView({ projectPath: _projectPath }: RegistryViewProps) {
  return (
    <div className="placeholder-view">
      Registry Browser (Phase 2)
    </div>
  );
}
