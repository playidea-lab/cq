interface DashboardViewProps {
  projectPath: string;
}

export function DashboardView({ projectPath: _projectPath }: DashboardViewProps) {
  return (
    <div className="placeholder-view">
      Dashboard (Phase 1 - coming next)
    </div>
  );
}
