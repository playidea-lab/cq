interface TimelineViewProps {
  projectPath: string;
}

export function TimelineView({ projectPath: _projectPath }: TimelineViewProps) {
  return (
    <div className="placeholder-view">
      Workflow Timeline (Phase 3)
    </div>
  );
}
