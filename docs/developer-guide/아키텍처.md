# C4 아키텍처

이 문서는 C4의 내부 구조와 컴포넌트 간 관계를 설명합니다.

## 패키지 구조

```
c4/
├── __init__.py
├── mcp_server.py       # MCP 서버 및 C4Daemon
├── state_machine.py    # 상태 머신
├── cli.py              # CLI 인터페이스
├── bundle.py           # 체크포인트 번들
├── validation.py       # 검증 실행기
│
├── models/             # Pydantic 데이터 모델
│   ├── __init__.py
│   ├── enums.py        # ProjectStatus, ExecutionMode 등
│   ├── task.py         # Task, TaskStatus
│   ├── worker.py       # WorkerInfo
│   ├── checkpoint.py   # CheckpointConfig, CheckpointState
│   ├── event.py        # Event, EventType
│   ├── state.py        # C4State
│   ├── queue.py        # TaskQueue
│   └── responses.py    # TaskAssignment, SubmitResponse 등
│
├── store/              # 상태 저장소 (Pluggable)
│   ├── __init__.py
│   ├── protocol.py     # StateStore, LockStore ABC
│   ├── exceptions.py   # StoreError, StateNotFoundError
│   ├── local_file.py   # LocalFileStateStore
│   └── sqlite.py       # SQLiteStateStore
│
├── daemon/             # 데몬 매니저들
│   ├── __init__.py
│   ├── locks.py        # LockManager (Scope Lock)
│   ├── workers.py      # WorkerManager
│   └── events.py       # SupervisorLoopManager
│
└── supervisor/         # Supervisor 백엔드 (Pluggable)
    ├── __init__.py
    ├── backend.py      # SupervisorBackend ABC
    ├── claude_backend.py  # Claude CLI 구현
    ├── mock_backend.py    # Mock 구현 (테스트용)
    └── prompt.py       # 프롬프트 렌더러
```

## 컴포넌트 다이어그램

```
┌─────────────────────────────────────────────────────────────┐
│                      Claude Code                            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                 MCP Protocol                         │   │
│  └─────────────────────────┬───────────────────────────┘   │
└────────────────────────────│────────────────────────────────┘
                             │
                             v
┌────────────────────────────────────────────────────────────┐
│                      MCP Server                            │
│  ┌─────────────────────────────────────────────────────┐  │
│  │                    C4Daemon                          │  │
│  │                                                      │  │
│  │   ┌──────────────┐    ┌──────────────┐              │  │
│  │   │ StateMachine │    │ TaskManager  │              │  │
│  │   └──────┬───────┘    └──────────────┘              │  │
│  │          │                                           │  │
│  │          v                                           │  │
│  │   ┌──────────────┐    ┌──────────────┐              │  │
│  │   │  StateStore  │    │ LockManager  │              │  │
│  │   │  (Protocol)  │    └──────────────┘              │  │
│  │   └──────┬───────┘                                  │  │
│  │          │                                           │  │
│  │   ┌──────┴───────┬──────────────┐                   │  │
│  │   v              v              v                    │  │
│  │ ┌────────┐  ┌────────┐  ┌──────────┐               │  │
│  │ │LocalFS │  │ SQLite │  │ Supabase │               │  │
│  │ └────────┘  └────────┘  └──────────┘               │  │
│  │                                                      │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                            │
│  ┌─────────────────────────────────────────────────────┐  │
│  │               SupervisorBackend                      │  │
│  │                  (Protocol)                          │  │
│  │                                                      │  │
│  │   ┌────────────┐    ┌────────────┐                  │  │
│  │   │ ClaudeCLI  │    │    Mock    │                  │  │
│  │   └────────────┘    └────────────┘                  │  │
│  └─────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────┘
```

## 핵심 컴포넌트

### C4Daemon

프로젝트 상태와 태스크 오케스트레이션을 관리하는 중앙 컨트롤러.

```python
class C4Daemon:
    def __init__(
        self,
        project_root: Path | None = None,
        state_store: StateStore | None = None,  # DI
    ):
        self.root = project_root or Path.cwd()
        self.state_machine = StateMachine(self.c4_dir, store=state_store)

    def initialize(self, project_id: str) -> C4State: ...
    def load(self) -> None: ...

    # MCP 도구 핸들러
    def get_task(self, worker_id: str) -> TaskAssignment: ...
    def submit_task(self, task_id, commit_sha, results) -> SubmitResponse: ...
```

### StateMachine

상태 전이와 불변 조건을 관리.

```python
class StateMachine:
    def __init__(self, c4_dir: Path, store: StateStore | None = None):
        self._store = store or LocalFileStateStore(c4_dir)

    def transition(self, event: str, actor: str) -> ProjectStatus:
        # 1. 전이 유효성 검사
        # 2. 불변 조건 확인
        # 3. 상태 변경
        # 4. 이벤트 기록
        # 5. 저장

    def check_gate_conditions(self, config: C4Config) -> str | None:
        # 체크포인트 조건 확인
```

### StateStore (Protocol)

상태 영속성을 위한 추상 인터페이스.

```python
class StateStore(ABC):
    @abstractmethod
    def load(self, project_id: str) -> C4State: ...

    @abstractmethod
    def save(self, state: C4State) -> None: ...

    @abstractmethod
    def exists(self, project_id: str) -> bool: ...

    @abstractmethod
    def delete(self, project_id: str) -> None: ...
```

**구현체**:
- `LocalFileStateStore`: 파일 기반 (`.c4/state.json`)
- `SQLiteStateStore`: SQLite 데이터베이스

### LockStore (Protocol)

분산 잠금을 위한 추상 인터페이스.

```python
class LockStore(ABC):
    @abstractmethod
    def acquire_scope_lock(
        self, project_id, scope, owner, ttl_seconds
    ) -> bool: ...

    @abstractmethod
    def release_scope_lock(self, project_id, scope) -> bool: ...

    @abstractmethod
    def refresh_scope_lock(
        self, project_id, scope, owner, ttl_seconds
    ) -> bool: ...
```

### SupervisorBackend (Protocol)

Supervisor 리뷰를 위한 추상 인터페이스.

```python
class SupervisorBackend(ABC):
    @abstractmethod
    def review_checkpoint(
        self, prompt: str, context: dict
    ) -> SupervisorResponse: ...
```

**구현체**:
- `ClaudeCliBackend`: Claude CLI 사용
- `MockBackend`: 테스트용 Mock

## 데이터 흐름

### 태스크 할당 흐름

```
Worker                    C4Daemon               StateMachine
  │                          │                        │
  │  c4_get_task(worker_id)  │                        │
  │────────────────────────▶│                        │
  │                          │  check_status()       │
  │                          │───────────────────────▶│
  │                          │◀───────────────────────│
  │                          │                        │
  │                          │  find_assignable_task()│
  │                          │───────────────────────▶│
  │                          │◀───────────────────────│
  │                          │                        │
  │                          │  acquire_lock(scope)   │
  │                          │───────────────────────▶│
  │                          │◀───────────────────────│
  │                          │                        │
  │  TaskAssignment          │                        │
  │◀────────────────────────│                        │
```

### Checkpoint 흐름

```
StateMachine          C4Daemon           Supervisor
     │                    │                   │
     │ gate_reached       │                   │
     │──────────────────▶│                   │
     │                    │                   │
     │                    │ create_bundle()   │
     │                    │──────────────────▶│
     │                    │                   │
     │                    │ review_checkpoint()
     │                    │──────────────────▶│
     │                    │                   │
     │                    │ SupervisorResponse│
     │                    │◀──────────────────│
     │                    │                   │
     │ transition(decision)                   │
     │◀──────────────────│                   │
```

## 확장 포인트

### 1. 커스텀 StateStore
새로운 저장소 백엔드 추가. [StateStore 확장](StateStore-확장.md) 참조.

### 2. 커스텀 SupervisorBackend
다른 LLM이나 리뷰 시스템 연동. [SupervisorBackend 확장](SupervisorBackend-확장.md) 참조.

### 3. 커스텀 Validator
새로운 검증 명령 추가. [커스텀 Validator](커스텀-Validator.md) 참조.

## 설계 원칙

### 1. Protocol 기반 추상화
- 구현이 아닌 인터페이스에 의존
- 테스트 용이성
- 백엔드 교체 가능

### 2. 의존성 주입
```python
# 기본 동작 (LocalFile)
daemon = C4Daemon(project_root)

# 커스텀 Store
store = SQLiteStateStore(db_path)
daemon = C4Daemon(project_root, state_store=store)
```

### 3. 상태 머신 패턴
- 명시적 상태 전이
- 불변 조건 강제
- 이벤트 소싱

### 4. Backward Compatibility
- 기존 코드 변경 없이 동작
- Optional 파라미터로 확장

## 다음 단계

- [StateStore 확장](StateStore-확장.md) - 커스텀 백엔드 구현
- [SupervisorBackend 확장](SupervisorBackend-확장.md) - 다른 LLM 연동
- [MCP 도구 레퍼런스](../api/MCP-도구-레퍼런스.md) - 저수준 API
