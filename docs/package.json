{
  "name": "cq-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "generate": "node scripts/generate-agent-files.mjs"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
